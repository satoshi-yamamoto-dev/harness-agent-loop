# プロジェクト固有情報

## プロジェクト概要

（このプロジェクトの目的・背景を記載）

## 技術スタック

（使用するフレームワーク・言語・インフラを記載）

## 開発サーバーの起動方法

（アプリの起動コマンドとURLを記載。evaluator が Playwright で接続するために必要）

## 備考

（プロジェクト固有の注意事項があれば記載）

## リポジトリ境界

- このプロジェクトで「コミット」「push」とだけ依頼された場合、対象は `harness-agent-loop` のみ。会話中に別リポジトリを編集していても、コミットや push の対象に含めない。
- 別リポジトリのコミットや push は、ユーザーがそのリポジトリを明示的に指定した場合だけ行う。
- コミット前に Git のルート、ブランチ、送信先、ステージ済みファイルを確認する。
