# このリポジトリでの作業範囲

- このリポジトリのタスクの標準の作業対象は、このファイルが置かれた `harness-agent-loop` リポジトリとする。
- ユーザーが「コミット」「push」とだけ依頼した場合、対象はこのリポジトリに限定する。会話中に別リポジトリを閲覧・編集していても、そこへのコミットや push の許可とは解釈しない。
- 別リポジトリのコミットや push は、ユーザーがそのリポジトリを明示的に指定した場合だけ行う。複数リポジトリを対象にする場合も、それぞれの指定を確認する。
- コミット前に Git のルート、ブランチ、送信先、ステージ済みファイルを確認し、対象外の変更を混ぜない。
